# Site audit notes

This branch contains bug fixes identified during the pre-production audit.

- Removed duplicate GitHub Pages deployment workflow.
- Added a complete HTML5 document structure and mobile viewport metadata.
- Repaired navigation and booking controls that pointed to `#` without an action.
- Marked the sample Square booking control as disabled until the owner's real Square Appointments account is connected.
- Added automated checks for document structure, placeholder links, and viewport metadata.
